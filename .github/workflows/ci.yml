name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy: 
      matrix: 
        target: [mac, linux, windows]
        include: 
          - target: windows
            os: ubuntu-latest
            upload: windows
            runtime: win-x64
          - target: linux
            os: ubuntu-latest
            upload: linux
            runtime: linux-x64
          - target: mac
            os: macos-latest
            upload: macos
            runtime: osx-x64
     
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4.2.0
      with:
        dotnet-version: 9.0.x

    - name: Install WASM-Tools
      run: dotnet workload install wasm-tools

    - name: Build SolisManager-${{matrix.target}}
      run: dotnet publish SolisManager.sln -c Release --runtime ${{matrix.runtime}} --self-contained true -p:PublishTrimmed=false --property:PublishDir=./artifacts/publish /p:Version=1.0.${{github.run_number}}
        
    - name: Zip the release
      run: zip -r ./artifacts/SolisManager-${{matrix.upload}}.zip ./artifacts/publish

    - name: Release
      uses: softprops/action-gh-release@v2
      with:
        files: ./artifacts/SolisManager-${{matrix.upload}}.zip
        name: Release 1.0.${{ github.run_number }}
        tag_name: 1.0.${{ github.run_number }}
        make_latest: true

